version: "3.9"

services:
  moodle-api:
    build: .
    image: ghcr.io/dotnaos/moodle-cli:latest
    command: ["serve", "--addr", ":8080"]
    environment:
      - PORT=8080
      - MOODLE_CLI_HOME=/data
    ports:
      - "8080:8080"
    volumes:
      - ${HOME}/.moodle-cli:/data
